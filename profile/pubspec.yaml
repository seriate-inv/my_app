name: profile_user
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # Fixed SDK constraint

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  shared_preferences: ^2.2.2  # Updated to latest stable
  intl_phone_field: ^3.1.0
  font_awesome_flutter: ^10.5.0
  # Removed smart_otp (not needed - we'll use manual OTP)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
